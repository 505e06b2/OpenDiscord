{
	"name": "OpenDiscord",
	"version": "0.0.1",
	"main": "index.js",
	"scripts": {
		"start": "electron . --ignore-gpu-blacklist --enable-gpu-rasterization --enable-oop-rasterization --enable-native-gpu-memory-buffers"
	},
	"dependencies": {
		"electron": "^9.3.1"
	}
}
